$kind: http-request
name: Create Subcategory
method: POST
url: '{{baseUrl}}/api/v1/subcategories'
order: 3000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "name": "Smartphones",
      "category": "{{categoryId}}"
    }
